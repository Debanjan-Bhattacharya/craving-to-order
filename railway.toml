[build]
builder = "nixpacks"

[deploy]
startCommand = "python build_dish_name_embeddings.py && python -m uvicorn api:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
